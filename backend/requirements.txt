# Backend Python dependencies.
#
# Direct dependencies are pinned (==) to the exact versions the test suite runs
# against, so CI, Docker, and Railway resolve the same set instead of drifting to
# whatever `>=` happens to be latest at build time (DEP-01).
#
# Two entries are intentionally NOT exact-pinned and carry a reason inline:
#   - uvloop: excluded on win32, so it is not installed/validated in the dev venv;
#     it is resolved only in the Linux build. Kept as a floor.
#   - aiohttp: a test-only Socket.IO transport (test_socket_e2e.py), not installed
#     in the dev venv. Kept as a floor.
#
# Transitive dependencies still float. A fully-hashed, universal transitive lock
# (e.g. `uv pip compile --universal --generate-hashes`) must be generated in the
# Linux build environment; that is tracked as the remaining DEP-01 item in
# E2E_AUDIT_REMEDIATION_PLAYBOOK.md. To bump a pin: edit it here and re-run the
# backend test suite.
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-socketio==5.16.2
python-chess==1.999
redis==8.0.0
python-telegram-bot==22.7
pydantic-settings==2.14.1
sqlalchemy==2.0.50
asyncpg==0.31.0
greenlet==3.5.1
alembic==1.18.4
python-dotenv==1.2.2
stripe==15.2.0
requests==2.34.2
pytest==9.0.3
pytest-asyncio==1.4.0
aiosqlite==0.22.1  # SQLite async driver for pure-unit DB tests (test_solvency.py)
aiohttp>=3.9.0  # test-only (socket E2E test_socket_e2e.py); not installed in dev venv, left as a floor
httpx==0.28.1
tonsdk==1.0.15
uvloop>=0.19.0; sys_platform != 'win32'  # Linux-only; not installed/validated in dev venv, left as a floor
